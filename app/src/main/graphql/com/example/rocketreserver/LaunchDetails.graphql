query LaunchDetails($id:ID!) {
  launch(id: $id) {
    ... on Launch @include(if: $withId){
      id
      site
    }
    mission {
      name
      missionPatch(size:LARGE)
    }
    rocket {
      name
      type
    }
    isBooked
  }
}